echo "Running pre-push checks..."

# Run Biome lint and format check across all files
bun run format:check || {
  echo "Format check failed. Run 'bun run format' to fix."
  exit 1
}

echo "Pre-push checks passed!"
