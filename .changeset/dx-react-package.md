---
"@frontal-labs/react": minor
---

New package: `useChat` (streams from any route returning `toUIMessageStreamResponse`, errors as data with `retry()`), `useAgentRun` (trigger + typed run stream), `useWorkflowApprovals` (list/approve/reject). Transport-agnostic; `react >= 18` peer.
