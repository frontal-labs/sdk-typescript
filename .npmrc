# Frontal SDK npm registry configuration
registry=https://registry.npmjs.org/

# Publish @frontal-labs packages to the public npm registry
@frontal-labs:registry=https://registry.npmjs.org/

# Save exact versions for reproducibility
save-exact=true
