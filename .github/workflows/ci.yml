name: CI (Pull Requests)
# This workflow runs on pull requests only.
# For main branch CI, see publish.yml which calls the same reusable workflows.

on:
  pull_request:
    paths-ignore:
      # Documentation
      - '**/*.md'
      - '**/docs/**'
      - 'LICENSE'

      # Editor/formatting configs
      - '**/.prettierrc'
      - '**/.prettierignore'
      - '**/.editorconfig'
      - '.vscode/**'

      # Git configs
      - '**/.gitignore'
      - '**/.gitattributes'

jobs:
  ci-core:
    name: Core Packages CI
    uses: ./.github/workflows/ci-core.yml
    permissions:
      actions: read
      contents: read
