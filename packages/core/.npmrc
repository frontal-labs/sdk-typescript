# @frontal/core package npm configuration
# Inherit root configuration with package-specific settings

# Ensure this package publishes to the correct registry
@frontal:registry=${NPM_REGISTRY:-https://registry.npmjs.org/}

# Package-specific publishing settings
publish_config_access=public

# Development overrides (if needed)
# registry=${DEV_REGISTRY:-https://registry.npmjs.org/}
# @frontal:registry=${DEV_REGISTRY:-https://registry.npmjs.org/}
